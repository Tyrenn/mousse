{
	"extends": "@core/tsconfig/base",
	"compilerOptions": {
		"rootDir": "./src",
		"outDir": "./dist",
		// Benchmarked frameworks ship type definitions incompatible between themselves : only check our own code
		"skipLibCheck": true
	},
	"include": ["src/**/*"]
}
